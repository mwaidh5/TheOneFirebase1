node_modules
node_modules/
*.zip
*.tar.gz
.env
.env.local
.env.*.local
the-one/dist/
GoogleService-Info.plist
# ...except the iOS app's, which must be committed so CI can bundle it.
!the-one/ios/App/App/GoogleService-Info.plist
